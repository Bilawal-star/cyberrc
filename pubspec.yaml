name: cyberrc
description: Cyber RC Controller
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  permission_handler: ^11.3.1
  flutter_blue_plus: ^1.31.15
  flutter_bluetooth_serial: ^0.4.0
  speech_to_text: ^6.6.0
  flutter_tts: ^4.0.2
  vibration: ^1.9.0

flutter:
  uses-material-design: true
